# Next Ads Feature Store Jobs

These entrypoints are the first opt-in home for Next Ads feature-store
materialisation.

The initial slice is contract-first: table registry, SQL contracts, setup
job, DAB resource, and validation tests. The build entrypoints currently
resolve and log the registry tables they own without changing existing
production outputs.

