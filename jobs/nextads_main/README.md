# NextAds Main Jobs

Current v1 Databricks job entrypoints should move here only after the reusable
package layer is stable.

Until then, scheduled job paths remain in `scripts/` and Databricks job
resources remain pointed at those existing paths.
