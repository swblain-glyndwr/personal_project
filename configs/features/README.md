# Feature Configs

Feature-layer settings, feature grains, keys, snapshot handling, and quality
policy config can live here once feature store work is implemented.

Current `config/` settings remain the active config path until compatibility is
implemented and tested.
