# Feature SQL

SQL used to define or materialise reusable feature tables can move here once
feature ownership, grain, keys, snapshot dates, and refresh approach are
defined.

Existing production SQL and table names should not be renamed as part of a
structure-only move.
